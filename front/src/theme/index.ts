import { colors } from '@/theme/tokens/colors.ts';
import { typography } from '@/theme/tokens/typography.ts';
import { scaleByType } from '@/utils/scale/scale';

export const theme = {
  colors,
  typography,
  scale:  scaleByType
}